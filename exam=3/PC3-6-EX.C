#include<stdio.h>
#include<conio.h>
 main()
{
	char plus='a',plus;

	clrscr();
	do
	{
		if(plus=='a' && plus=='e' && plus=='i' && plus=='m' plus=='q' && plus=='u' && plus=='y')
		{
		printf("%c",plus);
		}
	plus+=3;
	}while(plus<=z);

	getch();
}